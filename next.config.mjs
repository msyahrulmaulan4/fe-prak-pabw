// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  // async middleware() {
  //   const { Middleware } = require("./src/middleware");
  //   return Middleware;
  // },
};

export default nextConfig;
